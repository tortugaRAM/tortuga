# Pipe Align
ram.ai.new.mainState.Body:
  states:
    start:
      type: Start
    site:
      type: SonarNavigationTask
      init:
        success: end
        failure: end
        startDepth: 1.3
        shortDepth: 1.3
        endDepth: 1.3
        duration: 300
    end:
      type: End

  transitions:
    - [start, next, site]
